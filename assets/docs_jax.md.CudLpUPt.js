import{_ as i,o as a,c as e,a2 as t}from"./chunks/framework.D66Xq0Kv.js";const c=JSON.parse('{"title":"Running Linnet models in JAX","description":"","frontmatter":{},"headers":[],"relativePath":"docs/jax.md","filePath":"docs/jax.md","lastUpdated":null}'),n={name:"docs/jax.md"};function h(o,s,l,p,r,d){return a(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="running-linnet-models-in-jax" tabindex="-1">Running Linnet models in JAX <a class="header-anchor" href="#running-linnet-models-in-jax" aria-label="Permalink to &quot;Running Linnet models in JAX&quot;">​</a></h1><p><code>python/linnet_jax</code> is the JAX adapter: it runs a Linnet entry as a JAX function and exports a JAX function as Linnet source. Like the PyTorch adapter, it contains no model-specific code; everything it knows about a model comes from the compiler.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> python/linnet_jax</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --all-extras</span></span></code></pre></div><h2 id="linnet-to-jax" tabindex="-1">Linnet to JAX <a class="header-anchor" href="#linnet-to-jax" aria-label="Permalink to &quot;Linnet to JAX&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> jax</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> linnet_jax </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> load</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> load(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;examples/04-tiny-transformer/src/lib.linnet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    generics</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Vocab&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;H&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Heads&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Inner&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">256</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Layers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;T&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;f32&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    weights</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;weights/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># .safetensors file or directory, or {path: array}</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    std_root</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stdlib&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logits </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> jax.jit(model)(tokens, cos_table, sin_table)</span></span></code></pre></div><p><code>load</code> reads the plan of the root block to learn the entry&#39;s signature and parameter paths. Each time the function is called with a new combination of input shapes it asks the compiler for the StableHLO of the entry with those dimensions bound (<code>linnet stablehlo</code>), checks that every parameter the module names is present in the weights, and wraps the module as a <code>jax.export.Exported</code>. Calls then run under XLA and compose with <code>jax.jit</code>. Weights are plain array arguments of the wrapped module, bound by the <code>linnet.path</code> names the compiler attaches. Optional parameters must be all present or all absent. The wrapped module has no VJP: this is inference.</p><h2 id="jax-to-linnet" tabindex="-1">JAX to Linnet <a class="header-anchor" href="#jax-to-linnet" aria-label="Permalink to &quot;JAX to Linnet&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> linnet_jax </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> export_linnet</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">export_linnet(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    forward,                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># forward(params, *inputs)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    params,                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># pytree of arrays: nested dicts, lists of alike subtrees</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (tokens,),               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># example inputs fix shapes and dtypes</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;src/model.linnet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    weights</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;weights/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># optional: SafeTensors under the parameter paths</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    std_root</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stdlib&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>export_linnet</code> captures <code>forward</code> with <code>jax.export</code>, which produces StableHLO with static shapes, and hands the module to <code>import_stablehlo</code>, which is also public: it takes StableHLO text whose <code>@main</code> takes the flattened parameter leaves followed by the inputs (what <code>jax.export</code> prints for <code>forward(params, *inputs)</code>) and a parameter tree of arrays or <code>jax.ShapeDtypeStruct</code>s, so a module produced elsewhere imports without running JAX. The translation goes operation by operation into a Core IR plan: the parameter pytree becomes the block hierarchy (<code>param</code> leaves, <code>sub</code> members for nested dicts in sorted key order, a sub array for a list — or a dict keyed <code>0..n-1</code> — of identical subtrees, so parameter paths such as <code>layers.0.q</code> are the pytree paths), <code>forward</code> becomes the root block&#39;s <code>entry</code>, and each StableHLO operation becomes the Linnet primitive with the same meaning — elementwise arithmetic, <code>reshape</code>/<code>permute</code>/<code>broadcast_to</code>, slices and concatenation — or index notation: <code>dot_general</code> is a comprehension with a <code>sum</code> over the contracted axes, <code>reduce</code> a comprehension over the kept axes, and the row-lookup form of <code>gather</code> (<code>table[ids]</code>) an element lookup. Private helper functions JAX emits are inlined. <code>linnet emit</code> prints the plan as formatted source, which is checked before it is written.</p><p>The translation never guesses. An operation without a mapping (a custom call, a <code>chlo</code> composite such as <code>erf</code>, a general <code>gather</code>) stops the export with a message naming every such operation. When JAX&#39;s lowering carries something Linnet has no need for — the NaN out-of-bounds guard of <code>jnp.take</code>, since Linnet indices are checked — it is dropped and the fact is reported in <code>ExportResult.notes</code>. Non-splat constants folded into the graph become <code>buffer</code> members saved with the weights.</p><p>JAX lowers its library functions to primitives, and the translation recognizes the decompositions it knows and emits the standard-library operation instead: <code>jax.nn.softmax</code> (the exp-sub-max over a sum, over the last axis) becomes <code>std.nn.softmax::softmax</code>, <code>jax.nn.sigmoid</code>, <code>jax.nn.silu</code> and the tanh <code>jax.nn.gelu</code> become their <code>std.nn.activations</code> operations, and <code>x * rsqrt(mean(x * x) + eps) * w</code> becomes <code>std.nn.norm::rms_norm</code>. The match is structural — the same operations, the same operands, the axis and the constants the decomposition uses — so a variant with a different constant stays spelled out, and every recovery is listed in <code>ExportResult.notes</code>. The primitives it replaces are dropped by <code>linnet emit</code> as dead code.</p><h3 id="state" tabindex="-1">State <a class="header-anchor" href="#state" aria-label="Permalink to &quot;State&quot;">​</a></h3><p>An entry that touches <code>state</code> members compiles with the state threaded explicitly: the members it reads are extra inputs (<code>linnet.state</code> arguments after the parameters) and the members it assigns are extra results after the entry&#39;s own (<code>linnet.states</code> on <code>@main</code>). <code>load</code> follows that contract: such an entry is called as <code>function(*inputs, state=mapping)</code> and returns <code>(result, new_state)</code>, where both mappings go by parameter path and a missing input state starts at zeros. Entries without state keep the plain <code>function(*inputs)</code> shape.</p><h2 id="flax-nnx" tabindex="-1">Flax NNX <a class="header-anchor" href="#flax-nnx" aria-label="Permalink to &quot;Flax NNX&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> linnet_jax </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> load_nnx</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> load_nnx(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;src/model.linnet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">generics</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">weights</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;weights/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">std_root</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stdlib&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logits </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> model(tokens)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">graphdef, state </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nnx.split(model)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># state paths are the parameter paths</span></span></code></pre></div><p><code>to_nnx(function)</code> (and <code>load_nnx</code>, which is <code>load</code> followed by it) mirrors the block hierarchy as nested <code>nnx.Module</code>s: each <code>param</code> is an <code>nnx.Param</code>, each <code>buffer</code> an <code>nnx.Variable</code>, a <code>sub</code> member a child module, and a sub array an <code>nnx.List</code>, so <code>layers.0.attn.q</code> in the source is <code>layers/0/attn/q</code> in the module&#39;s state. Calling the module gathers the arrays it holds and runs the compiled entry on them (<code>LinnetFunction.apply(parameters, *inputs)</code> does the same from a plain mapping), so state that went through <code>nnx.split</code>/<code>nnx.merge</code>, a checkpoint, or sharding is what the call uses. Absent optional parameters are <code>None</code>. The entry has no VJP, so the module is for inference.</p><h2 id="round-trip" tabindex="-1">Round trip <a class="header-anchor" href="#round-trip" aria-label="Permalink to &quot;Round trip&quot;">​</a></h2><p><code>tests/test_round_trip.py</code> exports a small transformer written with <code>jnp</code>, lints and formats the result, loads it back with the exported weights, and compares against the original under <code>jax.jit</code>; a second export is byte-identical. It also runs the hand-written transformer example through <code>load</code> and compares with a <code>jnp</code> reference of the same architecture, so the same <code>.linnet</code> file now has PyTorch, XLA, and JAX executions that agree.</p><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pytest</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pyright</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ruff</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ruff</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> format</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --check</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests</span></span></code></pre></div>`,20)])])}const E=i(n,[["render",h]]);export{c as __pageData,E as default};
