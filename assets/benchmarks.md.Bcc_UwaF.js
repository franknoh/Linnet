import{_ as t,C as n,o as r,c as h,j as s,a,E as o,a2 as l}from"./chunks/framework.D66Xq0Kv.js";const F=JSON.parse('{"title":"Benchmarks","description":"","frontmatter":{},"headers":[],"relativePath":"benchmarks.md","filePath":"benchmarks.md","lastUpdated":1790149373000}'),d={name:"benchmarks.md"};function c(p,e,k,m,u,g){const i=n("BenchTable");return r(),h("div",null,[e[0]||(e[0]=s("h1",{id:"benchmarks",tabindex:"-1"},[a("Benchmarks "),s("a",{class:"header-anchor",href:"#benchmarks","aria-label":'Permalink to "Benchmarks"'},"​")],-1)),e[1]||(e[1]=s("p",null,[a("Linnet adds a compiler in front of the frameworks; these numbers show what that costs and what it does not. Every row is produced by "),s("code",null,"bench/run.py"),a(" in the repository, and the JSON it writes ("),s("code",null,"bench/results/latest.json"),a(") is what this page renders — nothing here is typed in by hand.")],-1)),o(i),e[2]||(e[2]=l(`<h2 id="what-is-measured" tabindex="-1">What is measured <a class="header-anchor" href="#what-is-measured" aria-label="Permalink to &quot;What is measured&quot;">​</a></h2><p>The models are the repository&#39;s examples with random weights at realistic widths: the Llama-style decoder (<code>examples/05-llama</code>) and GPT-2 (<code>examples/06-gpt2</code>). For each configuration the harness times:</p><ul><li><strong>PyTorch reference</strong> — the hand-written <code>torch</code> implementation from the test suite, eager, the baseline everything is compared against.</li><li><strong>PyTorch reference, compiled</strong> — the same code under <code>torch.compile</code>.</li><li><strong>Linnet → PyTorch</strong> — <code>linnet_torch.load(..., numerics=&quot;equivalent&quot;)</code>: the checked source materialized as an <code>nn.Module</code>, library operations dispatched to native PyTorch kernels.</li><li><strong>Linnet → XLA</strong> — <code>linnet_jax.load</code> under <code>jax.jit</code>: the source compiled through <code>linnet stablehlo</code> and run by XLA.</li></ul><p>Latency is the median of timed iterations after warm-up, with CUDA synchronized around each call; throughput is tokens per second for the <code>forward</code> entry over a full sequence and steps per second for the Llama <code>decode</code> entry (one token per step through the KV caches). Outputs of every variant are compared against the reference (<code>max |Δ|</code>) so a fast wrong number cannot appear in the table.</p><p>Compile and load times are reported separately: <code>linnet check</code> on the package, <code>load</code> (which runs <code>linnet plan</code> and builds the module), and the first XLA compile.</p><h2 id="reading-the-numbers" tabindex="-1">Reading the numbers <a class="header-anchor" href="#reading-the-numbers" aria-label="Permalink to &quot;Reading the numbers&quot;">​</a></h2><ul><li>Where Linnet → PyTorch matches the reference, the interpreter&#39;s overhead is paid once per call in Python and the arithmetic is the same kernels; the gap that remains is the dispatch cost of the interpreted Core IR, which shrinks as the model grows.</li><li>Linnet → XLA is a whole-program compile: no Python in the loop, fused elementwise chains, and static shapes per binding. It is the number to compare with <code>torch.compile</code>.</li><li><code>linnet check</code> is milliseconds; it never runs the model.</li></ul><h2 id="reproducing" tabindex="-1">Reproducing <a class="header-anchor" href="#reproducing" aria-label="Permalink to &quot;Reproducing&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> python/linnet_torch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --all-extras</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ../linnet_jax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --group</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ../..</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LINNET_BIN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">build/release/linnet</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bench/run.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --device</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cuda</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --out</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bench/results/latest.json</span></span></code></pre></div><p><code>bench/README.md</code> describes the configurations and the environment the published numbers came from.</p>`,10))])}const b=t(d,[["render",c]]);export{F as __pageData,b as default};
