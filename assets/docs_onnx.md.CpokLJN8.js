import{_ as s,o as t,c as i,a2 as a}from"./chunks/framework.D66Xq0Kv.js";const k=JSON.parse('{"title":"Importing ONNX graphs","description":"","frontmatter":{},"headers":[],"relativePath":"docs/onnx.md","filePath":"docs/onnx.md","lastUpdated":null}'),n={name:"docs/onnx.md"};function o(d,e,h,r,p,c){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="importing-onnx-graphs" tabindex="-1">Importing ONNX graphs <a class="header-anchor" href="#importing-onnx-graphs" aria-label="Permalink to &quot;Importing ONNX graphs&quot;">​</a></h1><p><code>python/linnet_onnx</code> turns an ONNX graph into Linnet source.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> python/linnet_onnx</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uv</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sync</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --all-extras</span></span></code></pre></div><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> linnet_onnx </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> import_onnx</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> import_onnx(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;model.onnx&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;src/model.linnet&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">weights</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;weights/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">std_root</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stdlib&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">print</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result.notes)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># anything the translation had to drop</span></span></code></pre></div><p><code>import_onnx</code> runs ONNX shape inference, then translates the graph node by node into a Core IR plan (docs/plan-format.md), which <code>linnet emit</code> prints as formatted source; the result is checked before it is written.</p><ul><li><strong>Parameters.</strong> Tensor initializers become <code>param</code>s. Their dotted names form the block hierarchy (<code>blocks.0.attn.qkv.weight</code> → <code>blocks: [Block; N]</code>, <code>attn: Attn</code>, <code>param qkv...</code>); consecutive numbered children with the same structure become a sub array, so parameter paths match the ONNX names. With <code>weights=</code>, the initializers are saved as SafeTensors under their ONNX names, plus <code>bindings.json</code> when a path had to change.</li><li><strong>Inputs.</strong> Graph inputs become the entry&#39;s inputs; named symbolic dimensions (<code>dim_param</code>) become the entry&#39;s generic parameters, so a graph exported with a dynamic batch and sequence imports as <code>forward&lt;batch: Dim, seq: Dim&gt;</code>.</li><li><strong>Shape arithmetic.</strong> <code>Shape</code>, <code>Gather</code>/<code>Concat</code>/<code>Unsqueeze</code> on shapes, and small integer constants are folded at import time into dimension expressions, so <code>Reshape</code>, <code>Slice</code>, <code>Expand</code>, and <code>ConstantOfShape</code> get compile-time shapes. Where ONNX&#39;s own inference gives up after such a reshape, the importer propagates shapes itself.</li><li><strong>Operations.</strong> Elementwise arithmetic, comparisons, <code>Where</code>, <code>Cast</code>, <code>Transpose</code>, <code>Reshape</code>, <code>Slice</code>, <code>Concat</code>, <code>Expand</code>, <code>Identity</code>, <code>Pow</code> with constant exponents, <code>Reciprocal</code>; <code>MatMul</code> and <code>Gemm</code> as <code>std.linalg::matmul</code> / <code>batched_matmul</code> (or a contraction in index notation when only the right operand is a matrix); <code>Softmax</code>, <code>LayerNormalization</code>, <code>Gelu</code> (tanh), <code>Sigmoid</code>, and <code>Relu</code> as their standard-library operations; <code>Gather</code> along axis 0 as an element lookup; the <code>Reduce*</code> family as comprehensions.</li><li><strong>Recovered decompositions.</strong> Exporters spell some library operations out as primitives. The importer recognizes the shapes it knows and emits the library operation instead: PyTorch&#39;s RMS norm (<code>Mul(Mul(x, Reciprocal(Sqrt(Add(ReduceMean(Pow(x, 2)), eps)))), w)</code> and its <code>Div</code>, <code>Mul(x, x)</code>, and <code>Pow(-0.5)</code> spellings) becomes <code>std.nn.norm::rms_norm</code>, <code>Mul(x, Sigmoid(x))</code> becomes <code>silu</code>, and <code>Div(Exp(Sub(x, ReduceMax(x))), ReduceSum(Exp(...)))</code> over the last axis becomes <code>softmax</code>. The match is structural — the same operation types, the same operands, the axis, and the constants — so a variant is left as written, and every recovery is listed in <code>result.notes</code>; the replaced nodes are dropped as dead code.</li></ul><p>Anything else — <code>Erf</code>, custom domains, <code>Gather</code> on other axes, symbolic broadcasting the checker cannot prove — stops the import with a message naming every such node. Nothing is executed and no weight enters the source.</p><h2 id="linnet-to-onnx" tabindex="-1">Linnet to ONNX <a class="header-anchor" href="#linnet-to-onnx" aria-label="Permalink to &quot;Linnet to ONNX&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">linnet</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> onnx</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --std</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stdlib</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Vocab=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">11</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> H=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Heads=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Inner=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">            --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Layers=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> T=f32</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> B=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bind</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> S=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            examples/04-tiny-transformer/src/lib.linnet</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> model.onnx.txt</span></span></code></pre></div><p><code>linnet onnx</code> takes the same options as <code>linnet stablehlo</code> and prints the entry as an ONNX model in the ONNX text format (<code>onnx.parser.parse_model</code> reads it; <code>onnx.save</code> turns it into a <code>.onnx</code> file). The graph is <code>main</code>; its inputs are the entry&#39;s inputs followed by the parameters of the block hierarchy as <code>param&lt;N&gt;</code>, and the model&#39;s <code>metadata_props</code> map each <code>linnet.path.param&lt;N&gt;</code> to its parameter path, so weights bind by name and the model itself carries none. Both exporters share one evaluator (<code>backend::export_graph</code>): the same static-shape lowering of calls, loops, and index notation feeds a <code>GraphTarget</code> that spells the primitives in one format or the other — <code>GatherND</code> for element lookups, <code>Range</code> for <code>iota</code>, <code>Reshape</code>+<code>Expand</code> (after a <code>Transpose</code> when axes reorder) for broadcasts, <code>ReduceSum</code>/<code>ReduceMax</code>/... with an <code>axes</code> input for reductions.</p><p>An entry that touches <code>state</code> members gets them threaded explicitly: the members it reads are inputs <code>state&lt;N&gt;</code> (mapped to their paths by <code>linnet.state.state&lt;N&gt;</code>), and the members it assigns are outputs <code>next_state&lt;N&gt;</code> after the entry&#39;s own <code>output&lt;N&gt;</code> results (mapped by <code>linnet.next_state.next_state&lt;N&gt;</code>), so a runtime feeds each call&#39;s outputs back as the next call&#39;s inputs. Entries returning a tuple have one output per element.</p><p><code>import_onnx</code> recognizes the metadata, so an exported model imports back into Linnet with its parameters intact.</p><p><code>tests/test_export.py</code> exports the tiny transformer, runs it under onnxruntime with the same weights as the PyTorch materializer, checks the outputs agree, and imports it back into Linnet that agrees too.</p><p><code>tests/test_import.py</code> builds a GPT-style graph with <code>onnx.helper</code> (dynamic batch and sequence, shape arithmetic feeding the reshapes, a causal mask as an input), imports it, loads the result through the PyTorch adapter with the graph&#39;s initializers, and compares it with a PyTorch reference; a second import is byte-identical. It also imports a model exported by <code>torch.onnx.export(..., dynamo=True)</code>.</p>`,14)])])}const m=s(n,[["render",o]]);export{k as __pageData,m as default};
